[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd backend && npm run build && npm run migrate && npm run seed && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[env]
NODE_ENV = "production"
